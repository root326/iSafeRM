# prometheus镜像

docker search prometheus
docker pull bitnami/prometheus:2.45.0
docker run -d -p 9090:9090 --name prometheus --restart=always \
    -v ~/prometheus-2.37.8.linux-amd64/prometheus.yml:/data/prometheus.yml bitnami/prometheus:2.45.0 \
    --config.file=/data/prometheus.yml



# 暴露被监控节点
docker search node-exporter
docker pull bitnami/node-exporter:1.6.0
docker run -d --name node-exporter --restart=always \
    -p 9100:9100 bitnami/node-exporter:1.6.0
